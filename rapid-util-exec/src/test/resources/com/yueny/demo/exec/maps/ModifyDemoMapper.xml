<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yueny.superclub.util.exec.mock.dao.mapper.IModifyDemoMapper">
    <cache/>
    
    <select id="selectAll" resultType="ModifyDemoEntry">
        select * from MODIFY_DEMO order by id;
    </select>
    
    <select id="selectIds" resultType="java.lang.Long">
        select ID from MODIFY_DEMO order by id;
    </select>
    
</mapper>
